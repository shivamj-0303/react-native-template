import { makeStyles } from '@rneui/themed';

export const useStyles = makeStyles({
  errorContainer: {
    display: 'flex',
    height: '100%',
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
